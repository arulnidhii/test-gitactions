on:
  schedule:
    - cron: '*/2 * * * *'
    
jobs:
  test_schedule:
    runs-on: ubuntu-latest
    steps:
      - name: Echo forget her and move on
        if: github.event.schedule != '*/2 * * * *'
        run: echo "Yes I will"
      
